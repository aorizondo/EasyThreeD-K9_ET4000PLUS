#
# Marlin/config.ini  ---  Custom ET4000+ (aorizondo)
#
# NO se usa como fuente de configuracion. Toda la config de esta impresora vive
# directamente en config/EasyThreeD/ET4000PLUS/Configuration.h y Configuration_adv.h.
#
# Esta seccion queda SIN overrides a proposito: el build corre pero no aplica
# ningun cambio encima de los headers (que son la unica fuente de verdad).
#
[config:base]
ini_use_config = base
